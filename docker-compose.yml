version: '3'
services:
  django:
    build: .
    container_name: 'pois_2nd_project'
    working_dir: '/root/'
    # environment: # 環境変数の設定
    tty: true
    volumes:
      - ./app:/root/app
    ports:
      - "8000:8000"